{
  "name": "commons-monorepo",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "validate:contrast": "pnpm --filter @21stgov/commons-tokens validate:contrast",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "prettier": "^3.9.5",
    "turbo": "^2.10.4",
    "typescript": "~5.9.0"
  }
}
