{
  "$schema": "https://commonsui.com/schema/registry-item.v1.json",
  "schemaVersion": "1",
  "name": "ambient-direction",
  "type": "registry:lib",
  "title": "Ambient direction",
  "description": "Makes Base UI components follow the DOM `dir` (like native components) by reading the resolved direction and feeding it to Base UI's DirectionProvider.",
  "status": "experimental",
  "useWhen": ["Wrapping a Base UI root so it follows dir=\"rtl\" without a manual DirectionProvider"],
  "avoidWhen": ["Native components (they read the DOM dir directly)"],
  "dependencies": ["@base-ui/react"],
  "registryDependencies": [],
  "compatibility": {
    "react": ">=19",
    "rtl": true,
    "forcedColors": true
  },
  "accessibility": {
    "standard": "not-applicable"
  },
  "files": [
    {
      "path": "src/lib/ambient-direction.tsx",
      "type": "registry:lib"
    }
  ]
}
